[package]
name = "provision"
version = "0.4.0"
edition = "2024"
default-run = "provision"
description = "A GUI tool that takes a fresh Windows machine from just installed to ready to use"
license = "MIT"
authors = ["Reuzehagel"]
repository = "https://github.com/Reuzehagel/provision"

[dependencies]
iced = { version = "0.14", features = ["tokio"] }
lucide-icons = "0.575"
rfd = "0.17"
serde = { version = "1", features = ["derive"] }
reqwest = { version = "0.13", default-features = false, features = ["rustls"] }
tokio = { version = "1", features = ["process", "io-util", "time", "fs"] }
serde_json = "1"
toml = "1"
sysinfo = "0.35"
open = "5"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_UI_Shell", "Win32_Foundation", "Win32_System_Registry"] }

[target.'cfg(windows)'.build-dependencies]
winres = "0.1"
